import { CustomValidationPipePipe } from './custom-validation-pipe.pipe';

describe('CustomValidationPipePipe', () => {
  it('should be defined', () => {
    expect(new CustomValidationPipePipe()).toBeDefined();
  });
});
